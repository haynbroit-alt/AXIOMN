[project]
name = "axiomn"
version = "0.1.0"
description = "AXIOMN: the universal intent mediation layer."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["axiomn*"]
